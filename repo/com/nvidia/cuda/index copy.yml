{{- range .tags -}}
---
id: com.nvidia.cuda.cu{{.cuda.major}}{{.cuda.minor}}.{{.os_release.id}}.{{.os_release.version_codename}}
base: nvcr.io/nvidia/cuda:{{tag}}
has:
  os_release:
    id: {{.os_release.id}}
    id_like: {{.os_release.id_like}}
    version_id: {{.os_release.version_id}}
    version_codename: {{.os_release.version_codename}}
  cuda:
    major: {{.cuda.major}}
    minor: {{.cuda.minor}}
    {{- if cudann}}cudann: {{.cudann}}{{- end -}}
{{- end -}}