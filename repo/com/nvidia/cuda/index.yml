{{range $.tags}}---
id: com.nvidia.cuda.cu{{.cuda.major}}{{.cuda.minor}}.{{.flavor}}{{ if .cudann }}.cudann{{.cudann}}{{end}}.{{.os_release.id}}.{{.os_release.version_codename}}
from: nvcr.io/nvidia/cuda:{{.cuda.major}}.{{.cuda.minor}}.{{.cuda.patch}}-{{if .cudann}}cudann-{{end}}{{.flavor}}-{{.os_release.id}}{{.os_release.version_id}}
has:
  os_release: {{.os_release | toJSON }}
  cuda: {{.cuda | toJSON }}{{if .cudann }}
  cudann: {{.cudann | toJSON }}{{end}}
{{end}}